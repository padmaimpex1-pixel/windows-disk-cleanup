---
description: "Use when generating files, exports, or scripts for this user; apply default storage and GitHub push targets."
---

User preferences:

1. Generated output files (CSV, JSON, exports, transformed data, session artifacts) should be saved by default to:
   `K:\5NGO Education For Bengali Immigrant Women Fund Foundation eBook\__4 website assets __`
   unless the user explicitly asks for a different path.

2. For scripts created/updated during work:
   - Default GitHub owner/account: `padmaimpex1-pixel`
   - When pushing, if repository is not specified, ask for repo name once and then push there.
   - Never push without explicit user intent to push.
